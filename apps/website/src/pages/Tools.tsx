import { Stub } from "./Stub";

export function Tools() {
  return (
    <Stub
      eyebrow="Tools & MCPs"
      title="Tools."
      body="MCP gateway: tool registry, last health probe, policy posture. Wired in S4."
    />
  );
}
