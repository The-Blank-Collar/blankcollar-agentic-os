# shared

Reserved for cross-package types and helpers (role enums, goal/run schemas,
zod/pydantic models, etc.). Empty in Phase 0 by design — populate as soon as
two packages need to share something.
